#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webpack-init/bin/webpack-init.js" "$@"
  ret=$?
else 
  node  "$basedir/../webpack-init/bin/webpack-init.js" "$@"
  ret=$?
fi
exit $ret
