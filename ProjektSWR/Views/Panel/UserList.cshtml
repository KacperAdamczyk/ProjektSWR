@model IEnumerable<ProjektSWR.Models.ApplicationUser>
@{
    ViewBag.Title = "Lista użytkowników";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" type="text/css" href="~/Content/Panel.css" />


<h2>@ViewBag.Title</h2>

<div>
    <div class="dropdown">
        <button class="dropbtn">Użytkownicy</button>
        <div class="dropdown-content">
            <a href="~/Panel/ManageUsers">Edytuj</a>
            <a href="~/Panel/LockUsers">Blokuj</a>
            <a href="~/Panel/UnlockUsers">Odblokuj</a>
            <a href="~/Panel/UserList">Lista użytkowników</a>
            <a href="~/Panel/ConfirmUsers">Autoryzuj</a>
        </div>
    </div>
    <div class="dropdown">
        <button class="dropbtn">Zdarzenia</button>
        <div class="dropdown-content">
            <a href="~/Panel/ManageEvents">Kalendarz</a>
            <a href="~/Panel/EventsList">Lista zdarzeń</a>
        </div>
    </div>
    <div class="dropdown">
        <button class="dropbtn">Forum</button>
        <div class="dropdown-content">
            <a href="/Forum/Cat?i=1">Forum główne</a>
            <a href="/Forum/Cat?i=2">Forum techniczne</a>
        </div>
    </div>
</div>



<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserConfirmed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LockoutEnabled)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserConfirmed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LockoutEnabled)
            </td>
            <td>
                @Html.ActionLink("Details", "UserProfile", new { id = item.Id })
            </td>
        </tr>
    }

</table>